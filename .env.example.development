# Development environment (local)
# SQLite file is used in development via PRISMA_DB_PROVIDER=sqlite and lib dev paths.

# Next
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=CHANGE_ME

# Dev using Supabase/Postgres for parity with production
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE
# No provider switch needed; schema uses postgresql

# Optional public base URL for QR codes and links
NEXT_PUBLIC_BASE_URL=http://localhost:3000

